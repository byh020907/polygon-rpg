# Derived Loop State

## Current Phase

RUNNING — Human이 2026-09-11에 Codex loop를 명시적으로 재개했다. OpenCode는 계속 pause 상태다.

## Active Execution Goal

Human Feedback Priority — Hero/Rival/Owner와 Core/Retrieval Arm의 실제 도입 배치 연결 다음으로 Ancient Machine Awakening을 기존 input lock·camera·눈 점등·불완전 조립·접속부 봉쇄 stage에 보존하면서 SVG master·Composition·Polygon depth renderer의 동일 runtime data로 연결한다. 기술 기준선과 실제 공급·reference/Composition 승인을 구분하고 실제 gameplay scale에서 검증한 뒤 Garage 0%로 전진한다.

## Current Evidence / Gap

- 기존 제어핵 SVG master에 interaction/socket/control-output anchor를 추가하고, 회수팔을 `dormant/captured/released` pose와 control/claw/rival anchor를 가진 수정 가능한 SVG master로 연결했다. authored Composition의 stable `world-control-core`/`world-retrieval-arm`이 실제 map stage item을 선택 신호로 사용하고, stage별 레거시 팔 조각은 최종 Polygon `RenderFrame`에서 제거된다. 모든 항목은 `runtime-baseline-unapproved`다.
- 제어핵 interaction anchor는 실제 `scrap-control-device` 좌표와 일치한다. 포획 pose의 집게 anchor는 현재 라이벌 머리 위 anchor에 붙고, 핵 회수 뒤 released pose는 집게를 40 world unit 이상 분리한다. 게임 진행·상호작용·저장 writer는 기존 `GameScene`/map patch에 남고 Composition은 렌더 자세만 투영한다.
- Codex in-app Browser 1280×720과 844×390에서 포획된 팔이 라이벌 쪽에 붙고 `INPUT LOCK · ARM RELEASE`에서 위·오른쪽으로 접혀 분리되는 장면을 확인했다. 작은 주인공/라이벌과 대형 폐병기 구도, HUD/touch 접근을 유지했고 최종 검증 포트의 console error는 없었다. 이는 reference/Composition 승인이 아니다.
- `test:intro` 32개 stage/interaction 검사, `test:systems`, `assets:check`, `test:visual`, `test:graphics`, lint/format, art handoff 588-resource/2,909-link 대조, release metadata와 `git diff --check` PASS. 독립 Sol/high verifier도 실제 Composition 주입·stage pose 교체·anchor·review/QA 노출·미승인 상태 분리를 PASS 판정했다. 전체 노출 공격·양방향·touch의 실제 입력 판독과 도입부 처음부터의 연속 플레이는 아직 남아 있다.

Human Feedback Priority Gap: REF-01 front/side/3/4 master SVG, representative/action key pose와 Core/Retrieval Arm의 승인 원본·전용 rival hook 접촉 anchor는 공급·승인되지 않았고 현재 결과는 기술 기준선이다. Ancient Machine Awakening·Garage 0%는 아직 같은 production 연결과 실제 화면 검증이 필요하다. INBOX의 전체 그래픽 재작업·횡/사선 베기·접촉·모바일/연속 플레이 원문은 미완료로 보존한다.

## Preserved Work Reference

일괄 몹 교체 시안은 Human의 동일 체형/비율 지적으로 중단했다. local `codex/enemy-silhouette-redesign` / `f4f350b37d081bfdc5863773f80521b4cccaf38f`는 미완료/test-failing 보존본이며 main 통합 대상이 아니다. 원본 첨부와 실패/중간 PNG는 artifacts/enemy-redesign에 보존한다.

OpenCode candidate `opencode/product-goal-loop/20260905142359-2188ab1adfbe` at `0d5a9dc` remains unmerged in `C:/Users/byh02/AppData/Local/ProductGoalLoop/OpenCode/d76ddb28cc8ea5fa/worktrees/20260905142359-2188ab1adfbe`. Its report remains in `.git/product-goal-loop/opencode/executions/20260905142359-2188ab1adfbe.json`.
