# Human Feedback Inbox

아직 해석하지 않은 제품 feedback만 이 queue에 둔다. 구현 Task, Execution Goal, backlog와 완료 이력을 기록하지 않는다.

## Pending

### 높은 우선순위: 모바일 설치형 PWA와 전체 오프라인 실행

현재 게임은 모바일 조작과 반응형 화면만 있고 실제 설치 가능한 PWA 구조가 없다. 다른 일반 Gap보다 우선순위를 높여 Android와 iPhone/iPad에서 홈 화면 앱으로 설치하고 첫 정상 로딩 이후 전체 게임을 오프라인으로 플레이할 수 있게 한다.

#### 설치 경험과 공식 지원 범위

- Android Chromium과 iPhone/iPad Safari를 공식 모바일 지원 범위로 삼는다. 데스크톱 Chromium 설치도 호환하되 핵심 QA는 Android와 iOS/iPadOS에 둔다.
- 메인 메뉴에 설치 가능한 환경에서만 `앱으로 설치` 버튼을 표시한다. 플레이 중 자동 배너나 설치 창으로 흐름을 방해하지 않는다.
- Android·Chromium에서는 사용자 버튼 입력으로 시스템 설치 창을 연다. iPhone·iPad에서는 같은 버튼이 `공유 → 홈 화면에 추가`의 짧은 단계별 안내를 보여 준다.
- 이미 standalone으로 실행 중이거나 설치 완료 상태에서는 설치 버튼을 숨긴다. 설치를 지원하지 않는 브라우저에는 작동하지 않는 버튼을 노출하지 않는다.
- 설치 앱은 주소창 없는 `standalone` 창으로 실행하고 가로 방향을 우선 요청한다. 지원 기기에서는 게임 시작 시 방향 잠금을 사용하고, 잠금이 불가능하면 기존 가로 회전 안내를 유지한다.
- 노치, 상태 표시줄과 홈 인디케이터는 CSS safe-area를 사용해 HUD와 모바일 조작계를 침범하지 않게 한다. 강제 전체화면은 기본값으로 사용하지 않고 사용자의 별도 선택으로만 제공한다.

#### 아이콘과 시작 화면

- 앱 아이콘과 설치 시작 화면은 주인공 실루엣을 중심으로 한다. 얇은 신체를 그대로 축소하지 않고 보호 고글, 공구 가방과 검처럼 주인공을 식별하는 형태를 과장해 작은 아이콘에서도 읽히게 한다.
- 현재 흑백 중심 폴리곤·벡터 컷아웃 방향을 사용하고 강조색은 한 곳에만 제한한다.
- 일반 아이콘과 maskable 아이콘을 필요한 크기로 제공하며 투명 여백이나 OS 마스크 때문에 주인공이 잘리지 않게 실제 런처 미리보기로 확인한다.

#### 첫 로딩 이후 전체 오프라인

- 사용자가 온라인 상태에서 게임을 한 번 정상적으로 끝까지 로딩한 뒤에는 메인 메뉴, 모든 지역, 전투, 대화, HUD, 저장·복구와 설치 자산을 네트워크 없이 사용할 수 있어야 한다.
- 아직 내려받지 않은 지역 때문에 오프라인 진행이 막히는 부분 캐시는 사용하지 않는다. 현재 배포 버전의 플레이 필수 정적 자산을 하나의 검증 가능한 버전 단위로 준비한다.
- Web App Manifest는 앱 이름, 짧은 이름, 설명, start URL, scope, standalone display, landscape orientation, theme/background color와 일반/maskable icon을 명시한다.
- HTTPS production과 localhost에서 Service Worker를 등록한다. 모바일 실기기 LAN 검증은 HTTPS 터널을 사용하며 비보안 HTTP에서 설치 가능하다고 오판하지 않는다.
- navigation 요청은 오프라인 앱 shell로 복구하고, 필수 module·CSS·아이콘·폰트·게임 자산은 versioned cache에서 제공한다. 외부 출처와 개발자 전용 상태를 무차별 캐시하지 않는다.
- Service Worker, manifest와 icon MIME·scope·start URL이 현재 정적 서버와 실제 배포 경로 모두에서 올바르게 제공되어야 한다.

#### 안전한 업데이트

- 새 버전을 발견하면 현재 플레이를 중단하지 않고 백그라운드에서 전체 새 버전을 준비한다. 일부 파일만 교체된 혼합 버전을 실행하지 않는다.
- 준비가 끝나면 메인 메뉴에 `새 버전 준비됨`과 적용 버튼을 표시한다. 전투·대화·시간 소비 행동 중에는 자동 reload하지 않는다.
- 사용자가 적용하면 현재 진행과 recovery slot 저장을 먼저 완료한 뒤 waiting worker를 활성화하고 한 번만 reload한다.
- 새 버전 설치가 실패하거나 필수 자산 검증이 끝나지 않으면 기존 정상 cache를 계속 사용하며 재시도 상태를 보여 준다. 이전 cache는 새 버전이 완전히 활성화된 뒤에만 정리한다.
- Service Worker 자체와 update check가 영구 cache되어 업데이트를 발견하지 못하는 구조를 만들지 않는다.

#### 저장과 캐시 분리

- Service Worker cache와 게임 진행 저장은 별도 책임이다. cache 삭제·업데이트·오프라인 복구가 정상 저장, 장비, D-DAY, recovery slot을 초기화하지 않는다.
- 새 버전이 저장 schema를 바꾸면 기존 version migration과 recovery 정책을 통해 원자적으로 변환한다. 변환 실패를 새 게임으로 위장하지 않는다.
- 앱 업데이트 전에 최신 진행을 저장하고, 업데이트 후 같은 origin의 현재 진행으로 이어져야 한다.
- 앱 삭제나 브라우저 데이터 삭제로 진행을 잃을 수 있는 플랫폼 경계는 설정/도움말에 짧게 안내하되 매 실행마다 경고하지 않는다.

#### 실패·오프라인 화면

- 최초 방문에서 아직 필수 자산이 준비되지 않은 채 오프라인이면 빈 화면이나 무한 로딩 대신 `첫 실행에는 연결이 필요합니다`와 재시도 행동을 보여 준다.
- 이전 정상 버전이 있으면 새 업데이트가 실패해도 이전 버전으로 게임을 시작할 수 있어야 한다.
- 캐시 손상, manifest 오류, Service Worker 등록 실패와 update waiting 상태를 사용자 저장과 분리된 진단 상태로 노출해 개발자 패널과 자동 검사에서 재현할 수 있게 한다.

#### 확인 기준

- manifest 유효성, 필요한 icon·maskable 영역, Service Worker scope/lifecycle, 필수 자산 목록과 cache version 전환을 자동 검사한다.
- 새 설치 상태에서 온라인 첫 로딩 → 앱 설치 → 앱 완전 종료 → 네트워크 차단 → standalone 재실행 → 저장된 진행 계속 흐름을 실제 모바일 크기에서 검증한다.
- 게임 플레이 중 새 버전을 준비해도 reload되지 않고, 메인 메뉴 적용 후 새 버전으로 한 번만 전환되며 기존 저장과 recovery slot이 보존되는지 검증한다.
- Android Chromium과 iPhone/iPad Safari에서 설치 안내, 아이콘, 시작 화면, 가로 방향, safe-area와 오프라인 재실행을 각각 확인한다. 데스크톱 설치는 호환 smoke test만 수행한다.
- 브라우저 개발 도구에서 단순히 manifest가 보이는 것만으로 완료 처리하지 않는다. 실제 설치 가능 판정, standalone 실행, 오프라인 전체 user flow와 업데이트 실패 복구 evidence가 있어야 한다.

## Feedback Guide

실제 제품을 사용하며 느낀 문제, 기대한 결과와 관찰한 상황을 가능한 한 원문에 가깝게 적는다.

- 좋은 예: `Dungeon이 한 화면 통로처럼 보여서 탐험하는 느낌이 없어.`
- 좋은 예: `공격이 막힌 건지 무적인 건지 화면에서 구분하기 어려워.`
- 좋은 예: `모바일에서 현재 목표가 안 보여서 어디로 가야 할지 모르겠어.`
- 피할 예: `MapManager class를 만들어.`
- 피할 예: `특정 Agent에게 구현 Task를 배정해.`
